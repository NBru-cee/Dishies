import React from "react";
import { Stack, Box, Typography, IconButton } from "@mui/material";

const MainMenuItem = () => {
      return <section>Main Menu Item</section>;
};

export default MainMenuItem;
